export interface SelectedFilters {
  topics: string[]
  persons: string[]
  orgs: string[]
  locs: string[]
}

export interface FilterState {
  topics: string[]
  persons: string[]
  orgs: string[]
  locs: string[]
}
